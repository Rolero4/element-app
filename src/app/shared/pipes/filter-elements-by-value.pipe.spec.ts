import { FilterElementsByValuePipe } from './filter-elements-by-value.pipe';

describe('FilterElementsByValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterElementsByValuePipe();
    expect(pipe).toBeTruthy();
  });
});
